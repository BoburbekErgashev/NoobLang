//Token types
public enum TokenType{
    KEYWORD,
    IDENTIFIER,
    INT,
    STRING,
    LBRACE,
    RBRACE,
    LPAREN,
    RPAREN,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    ASSIGN,
    EQUALS,
    SEMICOLON,
    LESS_THAN,
    GREATER_THAN,
    MODULO,
    EOF
}
