//Token types
public enum TokenType{
    KEYWORD,
    IDENTIFIER,
    INT,
    STRING,
    LBRACE,
    RBRACE,
    LPAREN,
    RPAREN,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    ASSIGN,
    SEMICOLON,
    LESS_THAN,
    GREATER_THAN
}
